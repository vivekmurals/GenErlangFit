% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ErlangFuncs.R
\name{Erlang_Fit_v2_Pvalue}
\alias{Erlang_Fit_v2_Pvalue}
\title{Bootstrap-Based Goodness-of-Fit for Erlang Distribution}
\usage{
Erlang_Fit_v2_Pvalue(
  empiricaldata,
  k_star,
  lambda_star,
  s = length(empiricaldata),
  n = 1000,
  alpha = 0.05,
  pvaloption = "KS",
  ShowFigures = TRUE
)
}
\arguments{
\item{empiricaldata}{Numeric vector of observed data.}

\item{k_star}{Numeric, fitted Erlang shape parameter.}

\item{lambda_star}{Numeric, fitted Erlang scale parameter.}

\item{s}{Integer, number of observations. Defaults to \code{length(empiricaldata)}.}

\item{n}{Integer, number of bootstrap samples. Default = 1000.}

\item{alpha}{Numeric, significance level. Default = 0.05.}

\item{pvaloption}{Character. Choice of test statistic: \code{"KS"}, \code{"CvM"}, or \code{"AD"}.}

\item{ShowFigures}{Logical. If TRUE, produces optional plots (currently suppressed).}
}
\value{
A list containing:
\describe{
\item{p_value}{Bootstrap-estimated p-value.}
\item{q_value}{Binary indicator (1 = fail to reject, 0 = reject).}
\item{metric_star}{Observed test statistic.}
\item{sample_stats}{Bootstrap sample statistics.}
}
}
\description{
Computes a bootstrap-based p-value for the Erlang model fit
using Kolmogorov–Smirnov, Cramér–von Mises, or Anderson–Darling statistics.
}
\details{
The function compares the empirical CDF of the observed data to the
theoretical Erlang CDF and computes a test statistic. Bootstrap samples
from the fitted Erlang model are used to estimate the null distribution
and compute the corresponding p-value.
}
\examples{
\dontrun{
data <- rexp(200, rate = 0.5)
fit <- Erlang_Fit_v2(data, ShowFigures = FALSE)
pvals <- Erlang_Fit_v2_Pvalue(data, fit$Best$K_star, fit$Best$Lambda_star)
}

}
\seealso{
\code{\link[=Erlang_Fit_v2]{Erlang_Fit_v2()}}, \code{\link[=Erlang_NegLogLikelihood]{Erlang_NegLogLikelihood()}}
}
\keyword{internal}
